close(con)
if (lines %% 4 != 0) stop("Invalid FASTQ format")
reads <- lines / 4
reads_per_sample <-floor(reads / num_files)
cat("Total reads:", reads, "\n")
cat("Reads per split file:", reads_per_sample, "\n\n")
# -------- Prepare output --------
output_dir <- "~/chunk"
dir.create(output_dir, showWarnings = FALSE)
suffix <- "AGRF"
sample_ids <- sprintf("XY%03d-%s", seq_len(num_files), suffix)
# -------- Streaming --------
stream1 <- FastqStreamer(r1_file, n = reads_per_sample)
stream2 <- FastqStreamer(r2_file, n = reads_per_sample)
for (sample in sample_ids) {
fq1 <- yield(stream1)
fq2 <- yield(stream2)
if (length(fq1) == 0) break
writeFastq(fq1, file.path(output_dir, paste0(sample, "_R1.fastq.gz")))
writeFastq(fq2, file.path(output_dir, paste0(sample, "_R2.fastq.gz")))
cat("Written", sample, "with", length(fq1), "reads\n")
}
close(stream1)
close(stream2)
}
option_list <- list(
make_option(c("-n", "--number"), type="integer", default=1),
make_option(c("-p", "--path"), type="character", help="File containing FASTQ paths")
)
opts <- parse_args(OptionParser(option_list = option_list))
option_list <- list(
make_option(c("-n", "--number"), type="integer", default=1),
make_option(c("-p", "--path"), type="character", help="File containing FASTQ paths")
)
library(optparse)
option_list <- list(
make_option(c("-n", "--number"), type="integer", default=1),
make_option(c("-p", "--path"), type="character", help="File containing FASTQ paths")
)
opts <- parse_args(OptionParser(option_list = option_list))
(optparse)
source("~/mk_vcf/split_cli.R", echo = TRUE)
print(otps)
print(opts)
suppressPackageStartupMessages(
library(optparse))
source("slice_the_pie.R")
suppressPackageStartupMessages({
library(ShortRead)
library(seqinr)
})
seperate_fastq <- function(file, num_files) {
# Read FASTQ paths
paths <- readLines(file)
r1_file <- grep("_R1_", paths, value = TRUE)
r2_file <- grep("_R2_", paths, value = TRUE)
if (length(r1_file) != 1 || length(r2_file) != 1) {
stop("Expected exactly one R1 and one R2 file")
}
# -------- Count reads from R1 only --------
con <- file(r1_file, "r")
lines <- 0
repeat {
z <- readLines(con, n = 100000)
if (length(z) == 0) break
lines <- lines + length(z)
}
close(con)
if (lines %% 4 != 0) stop("Invalid FASTQ format")
reads <- lines / 4
reads_per_sample <-floor(reads / num_files)
cat("Total reads:", reads, "\n")
cat("Reads per split file:", reads_per_sample, "\n\n")
# -------- Prepare output --------
output_dir <- "~/chunk"
dir.create(output_dir, showWarnings = FALSE)
suffix <- "AGRF"
sample_ids <- sprintf("XY%03d-%s", seq_len(num_files), suffix)
# -------- Streaming --------
stream1 <- FastqStreamer(r1_file, n = reads_per_sample)
stream2 <- FastqStreamer(r2_file, n = reads_per_sample)
for (sample in sample_ids) {
fq1 <- yield(stream1)
fq2 <- yield(stream2)
if (length(fq1) == 0) break
writeFastq(fq1, file.path(output_dir, paste0(sample, "_R1.fastq.gz")))
writeFastq(fq2, file.path(output_dir, paste0(sample, "_R2.fastq.gz")))
cat("Written", sample, "with", length(fq1), "reads\n")
}
close(stream1)
close(stream2)
}
suppressPackageStartupMessages(
library(optparse))
suppressPackageStartupMessages(source("slice_the_pie.R", local = TRUE))
suppressPackageStartupMessages({
library(optparse)
source("slice_the_pie.R")
})
suppressPackageStartupMessages({  library(optparse)})
suppressMessages({source("slice_the_pie.R")})
library(ShortRead)
library(seqinr)
suppressPackageStartupMessages({
library(ShortRead)
library(seqinr)
})
seperate_fastq <- function(file, num_files) {
# Read FASTQ paths
paths <- readLines(file)
r1_file <- grep("_R1_", paths, value = TRUE)
r2_file <- grep("_R2_", paths, value = TRUE)
if (length(r1_file) != 1 || length(r2_file) != 1) {
stop("Expected exactly one R1 and one R2 file")
}
# -------- Count reads from R1 only --------
con <- file(r1_file, "r")
lines <- 0
repeat {
z <- readLines(con, n = 100000)
if (length(z) == 0) break
lines <- lines + length(z)
}
close(con)
if (lines %% 4 != 0) stop("Invalid FASTQ format")
reads <- lines / 4
reads_per_sample <-floor(reads / num_files)
cat("Total reads:", reads, "\n")
cat("Reads per split file:", reads_per_sample, "\n\n")
# -------- Prepare output --------
output_dir <- "~/chunk"
dir.create(output_dir, showWarnings = FALSE)
suffix <- "AGRF"
sample_ids <- sprintf("XY%03d-%s", seq_len(num_files), suffix)
# -------- Streaming --------
stream1 <- FastqStreamer(r1_file, n = reads_per_sample)
stream2 <- FastqStreamer(r2_file, n = reads_per_sample)
for (sample in sample_ids) {
fq1 <- yield(stream1)
fq2 <- yield(stream2)
if (length(fq1) == 0) break
writeFastq(fq1, file.path(output_dir, paste0(sample, "_R1.fastq.gz")))
writeFastq(fq2, file.path(output_dir, paste0(sample, "_R2.fastq.gz")))
cat("Written", sample, "with", length(fq1), "reads\n")
}
close(stream1)
close(stream2)
}
sPackageStartupMessages({  library(optparse)})
suppressPackageStartupMessages({  library(optparse)})
suppressMessages({source("slice_the_pie.R")})
suppressPackageStartupMessages({library(optparse)})
suppressMessages({source("slice_the_pie.R")})
suppressPackageStartupMessages({
library(ShortRead)
library(seqinr)
library(optparse)
})
seperate_fastq <- function(file, num_files) {
# Read FASTQ paths
paths <- readLines(file)
r1_file <- grep("_R1_", paths, value = TRUE)
r2_file <- grep("_R2_", paths, value = TRUE)
if (length(r1_file) != 1 || length(r2_file) != 1) {
stop("Expected exactly one R1 and one R2 file")
}
# -------- Count reads from R1 only --------
con <- file(r1_file, "r")
lines <- 0
repeat {
z <- readLines(con, n = 100000)
if (length(z) == 0) break
lines <- lines + length(z)
}
close(con)
if (lines %% 4 != 0) stop("Invalid FASTQ format")
reads <- lines / 4
reads_per_sample <-floor(reads / num_files)
cat("Total reads:", reads, "\n")
cat("Reads per split file:", reads_per_sample, "\n\n")
# -------- Prepare output --------
output_dir <- "~/chunk"
dir.create(output_dir, showWarnings = FALSE)
suffix <- "AGRF"
sample_ids <- sprintf("XY%03d-%s", seq_len(num_files), suffix)
# -------- Streaming --------
stream1 <- FastqStreamer(r1_file, n = reads_per_sample)
stream2 <- FastqStreamer(r2_file, n = reads_per_sample)
for (sample in sample_ids) {
fq1 <- yield(stream1)
fq2 <- yield(stream2)
if (length(fq1) == 0) break
writeFastq(fq1, file.path(output_dir, paste0(sample, "_R1.fastq.gz")))
writeFastq(fq2, file.path(output_dir, paste0(sample, "_R2.fastq.gz")))
cat("Written", sample, "with", length(fq1), "reads\n")
}
close(stream1)
close(stream2)
}
suppressMessages({source("slice_the_pie.R")})
option_list <- list(
make_option(c("-n", "--number"), type="integer", default=1),
make_option(c("-p", "--path"), type="character", help="File containing FASTQ paths")
)
opts <- parse_args(OptionParser(option_list = option_list))
# This ensures the user **must provide the path**
if (is.null(opts$path)) {
stop("ERROR: --path is required", call. = FALSE)
}
seperate_fastq(opts$path, opts$number)
# ------------------- Suppress all startup messages -------------------
suppressPackageStartupMessages({
library(optparse)
library(ShortRead)
library(seqinr)
})
# ------------------- Source external code silently -------------------
suppressMessages({
source("slice_the_pie.R")  # if this file also loads packages, messages are suppressed
})
# ------------------- Define the FASTQ splitting function -------------------
seperate_fastq <- function(file, num_files) {
# Read FASTQ paths
paths <- readLines(file)
r1_file <- grep("_R1_", paths, value = TRUE)
r2_file <- grep("_R2_", paths, value = TRUE)
if (length(r1_file) != 1 || length(r2_file) != 1) {
stop("Expected exactly one R1 and one R2 file")
}
# -------- Count reads from R1 only --------
con <- file(r1_file, "r")
lines <- 0
repeat {
z <- readLines(con, n = 100000)
if (length(z) == 0) break
lines <- lines + length(z)
}
close(con)
if (lines %% 4 != 0) stop("Invalid FASTQ format")
reads <- lines / 4
reads_per_sample <- floor(reads / num_files)
cat("Total reads:", reads, "\n")
cat("Reads per split file:", reads_per_sample, "\n\n")
# -------- Prepare output --------
output_dir <- "~/chunk"
dir.create(output_dir, showWarnings = FALSE)
suffix <- "AGRF"
sample_ids <- sprintf("XY%03d-%s", seq_len(num_files), suffix)
# -------- Streaming --------
stream1 <- FastqStreamer(r1_file, n = reads_per_sample)
stream2 <- FastqStreamer(r2_file, n = reads_per_sample)
for (sample in sample_ids) {
fq1 <- yield(stream1)
fq2 <- yield(stream2)
if (length(fq1) == 0) break
writeFastq(fq1, file.path(output_dir, paste0(sample, "_R1.fastq.gz")))
writeFastq(fq2, file.path(output_dir, paste0(sample, "_R2.fastq.gz")))
cat("Written", sample, "with", length(fq1), "reads\n")
}
close(stream1)
close(stream2)
}
# ------------------- Parse command-line arguments -------------------
option_list <- list(
make_option(c("-n", "--number"), type="integer", default=1),
make_option(c("-p", "--path"), type="character", help="File containing FASTQ paths")
)
opts <- parse_args(OptionParser(option_list = option_list))
# Ensure the user provides the path
if (is.null(opts$path)) {
stop("ERROR: --path is required", call. = FALSE)
}
# ------------------- Run the splitting function -------------------
seperate_fastq(opts$path, opts$number)
library(optparse)
# ------------------- Suppress all startup messages -------------------
suppressPackageStartupMessages({
library(optparse)
library(ShortRead)
library(seqinr)
})
# ------------------- Source external code silently -------------------
suppressMessages({
source("slice_the_pie.R")  # if this file also loads packages, messages are suppressed
})
# ------------------- Define the FASTQ splitting function -------------------
seperate_fastq <- function(file, num_files) {
# Read FASTQ paths
paths <- readLines(file)
r1_file <- grep("_R1_", paths, value = TRUE)
r2_file <- grep("_R2_", paths, value = TRUE)
if (length(r1_file) != 1 || length(r2_file) != 1) {
stop("Expected exactly one R1 and one R2 file")
}
# -------- Count reads from R1 only --------
con <- file(r1_file, "r")
lines <- 0
repeat {
z <- readLines(con, n = 100000)
if (length(z) == 0) break
lines <- lines + length(z)
}
close(con)
if (lines %% 4 != 0) stop("Invalid FASTQ format")
reads <- lines / 4
reads_per_sample <- floor(reads / num_files)
cat("Total reads:", reads, "\n")
cat("Reads per split file:", reads_per_sample, "\n\n")
# -------- Prepare output --------
output_dir <- "~/chunk"
dir.create(output_dir, showWarnings = FALSE)
suffix <- "AGRF"
sample_ids <- sprintf("XY%03d-%s", seq_len(num_files), suffix)
# -------- Streaming --------
stream1 <- FastqStreamer(r1_file, n = reads_per_sample)
stream2 <- FastqStreamer(r2_file, n = reads_per_sample)
for (sample in sample_ids) {
fq1 <- yield(stream1)
fq2 <- yield(stream2)
if (length(fq1) == 0) break
writeFastq(fq1, file.path(output_dir, paste0(sample, "_R1.fastq.gz")))
writeFastq(fq2, file.path(output_dir, paste0(sample, "_R2.fastq.gz")))
cat("Written", sample, "with", length(fq1), "reads\n")
}
close(stream1)
close(stream2)
}
# ------------------- Parse command-line arguments -------------------
option_list <- list(
make_option(c("-n", "--number"), type="integer", default=1),
make_option(c("-p", "--path"), type="character", help="File containing FASTQ paths")
)
opts <- parse_args(OptionParser(option_list = option_list))
# Ensure the user provides the path
if (is.null(opts$path)) {
stop("ERROR: --path is required", call. = FALSE)
}
# ------------------- Run the splitting function -------------------
seperate_fastq(opts$path, opts$number)
seperate_fastq <- function(file, num_files) {
# Read FASTQ paths
paths <- readLines(file)
r1_file <- grep("_R1_", paths, value = TRUE)
r2_file <- grep("_R2_", paths, value = TRUE)
if (length(r1_file) != 1 || length(r2_file) != 1) {
stop("Expected exactly one R1 and one R2 file")
}
# -------- Count reads from R1 only --------
con <- file(r1_file, "r")
lines <- 0
repeat {
z <- readLines(con, n = 100000)
if (length(z) == 0) break
lines <- lines + length(z)
}
close(con)
if (lines %% 4 != 0) stop("Invalid FASTQ format")
reads <- lines / 4
reads_per_sample <-floor(reads / num_files)
cat("Total reads:", reads, "\n")
cat("Reads per split file:", reads_per_sample, "\n\n")
# -------- Prepare output --------
output_dir <- "~/chunk"
dir.create(output_dir, showWarnings = FALSE)
suffix <- "AGRF"
sample_ids <- sprintf("XY%03d-%s", seq_len(num_files), suffix)
# -------- Streaming --------
stream1 <- FastqStreamer(r1_file, n = reads_per_sample)
stream2 <- FastqStreamer(r2_file, n = reads_per_sample)
for (sample in sample_ids) {
fq1 <- yield(stream1)
fq2 <- yield(stream2)
if (length(fq1) == 0) break
writeFastq(fq1, file.path(output_dir, paste0(sample, "_R1.fastq.gz")))
writeFastq(fq2, file.path(output_dir, paste0(sample, "_R2.fastq.gz")))
cat("Written", sample, "with", length(fq1), "reads\n")
}
close(stream1)
close(stream2)
}
suppressMessages({source("slice_the_pie.R")})
option_list <- list(
make_option(c("-n", "--number"), type="integer", default=1),
make_option(c("-p", "--path"), type="character", help="File containing FASTQ paths")
)
suppressMessages({source("slice_the_pie.R")})
option_list <- list(
make_option(c("-n", "--number"), type="integer", default=1),
make_option(c("-p", "--path"), type="character", help="File containing FASTQ paths"),
make_option(c("-d" , "--dir") , type = "character" , help = "Where you want your files"),
make_option(c("-s","--suffix"),type = "character" , help = "The suffix or part of your patient name")
)
opts <- parse_args(OptionParser(option_list = option_list))
seperate_fastq <- function(file, num_files , dir_name, suffix) {
# Read FASTQ paths
paths <- readLines(file)
r1_file <- grep("_R1_", paths, value = TRUE)
r2_file <- grep("_R2_", paths, value = TRUE)
if (length(r1_file) != 1 || length(r2_file) != 1) {
stop("Expected exactly one R1 and one R2 file")
}
# -------- Count reads from R1 only --------
con <- file(r1_file, "r")
lines <- 0
repeat {
z <- readLines(con, n = 100000)
if (length(z) == 0) break
lines <- lines + length(z)
}
close(con)
if (lines %% 4 != 0) stop("Invalid FASTQ format")
reads <- lines / 4
reads_per_sample <-floor(reads / num_files)
cat("Total reads:", reads, "\n")
cat("Reads per split file:", reads_per_sample, "\n\n")
# -------- Prepare output --------
dir.create(dir_name, showWarnings = FALSE)
sample_ids <- sprintf("XY%03d-%s", seq_len(num_files), suffix)
# -------- Streaming --------
stream1 <- FastqStreamer(r1_file, n = reads_per_sample)
stream2 <- FastqStreamer(r2_file, n = reads_per_sample)
for (sample in sample_ids) {
fq1 <- yield(stream1)
fq2 <- yield(stream2)
if (length(fq1) == 0) break
writeFastq(fq1, file.path(dir_name, paste0(sample, "_R1.fastq.gz")))
writeFastq(fq2, file.path(dir_name, paste0(sample, "_R2.fastq.gz")))
cat("Written", sample, "with", length(fq1), "reads\n")
}
close(stream1)
close(stream2)
}
suppressMessages({source("slice_the_pie.R")})
option_list <- list(
make_option(c("-n", "--number"), type="integer", default=1),
make_option(c("-p", "--path"), type="character", help="File containing FASTQ paths"),
make_option(c("-d" , "--dir") , type = "character" , help = "Where you want your files"),
make_option(c("-s","--suffix"),type = "character" , help = "The suffix or part of your patient name")
)
opts <- parse_args(OptionParser(option_list = option_list))
suppressMessages({source("slice_the_pie.R")})
option_list <- list(
make_option(c("-n", "--number"), type="integer", default=1),
make_option(c("-p", "--path"), type="character", help="File containing FASTQ paths"),
make_option(c("-d" , "--dir") , type = "character" , help = "Where you want your files"),
make_option(c("-s","--suffix"),type = "character" , help = "The suffix or part of your patient name")
)
opts <- parse_args(OptionParser(option_list = option_list))
seperate_fastq(opts$path, opts$number)
suppressPackageStartupMessages({
library(ShortRead)
library(seqinr)
library(optparse)
})
seperate_fastq <- function(file, num_files , dir_name, suffix) {
# Read FASTQ paths
paths <- readLines(file)
r1_file <- grep("_R1_", paths, value = TRUE)
r2_file <- grep("_R2_", paths, value = TRUE)
if (length(r1_file) != 1 || length(r2_file) != 1) {
stop("Expected exactly one R1 and one R2 file")
}
# -------- Count reads from R1 only --------
con <- file(r1_file, "r")
lines <- 0
repeat {
z <- readLines(con, n = 100000)
if (length(z) == 0) break
lines <- lines + length(z)
}
close(con)
if (lines %% 4 != 0) stop("Invalid FASTQ format")
reads <- lines / 4
reads_per_sample <-floor(reads / num_files)
cat("Total reads:", reads, "\n")
cat("Reads per split file:", reads_per_sample, "\n\n")
# -------- Prepare output --------
dir.create(dir_name, showWarnings = FALSE)
sample_ids <- sprintf("XY%03d-%s", seq_len(num_files), suffix)
# -------- Streaming --------
stream1 <- FastqStreamer(r1_file, n = reads_per_sample)
stream2 <- FastqStreamer(r2_file, n = reads_per_sample)
for (sample in sample_ids) {
fq1 <- yield(stream1)
fq2 <- yield(stream2)
if (length(fq1) == 0) break
writeFastq(fq1, file.path(dir_name, paste0(sample, "_R1.fastq.gz")))
writeFastq(fq2, file.path(dir_name, paste0(sample, "_R2.fastq.gz")))
cat("Written", sample, "with", length(fq1), "reads\n")
}
close(stream1)
close(stream2)
}
suppressMessages({source("slice_the_pie.R")})
option_list <- list(
make_option(c("-n", "--number"), type="integer", default=1),
make_option(c("-p", "--path"), type="character", help="File containing FASTQ paths"),
make_option(c("-d" , "--dir") , type = "character" , help = "Where you want your files"),
make_option(c("-s","--suffix"),type = "character" , help = "The suffix or part of your patient name")
)
opts <- parse_args(OptionParser(option_list = option_list))
opts$dir
source("~/mk_vcf/split_cli.R", echo = TRUE)
suppressMessages({source("slice_the_pie.R")})
option_list <- list(
make_option(c("-n", "--number"), type="integer", default=1),
make_option(c("-p", "--path"), type="character", help="File containing FASTQ paths"),
make_option(c("-d" , "--dir") , type = "character" , help = "Where you want your files"),
make_option(c("-s","--suffix"),type = "character" , help = "The suffix or part of your patient name")
)
opts <- parse_args(OptionParser(option_list = option_list))
seperate_fastq(opts$path, opts$number , opts$dir , opts$suffix)
