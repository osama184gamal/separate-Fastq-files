source("~/mk_vcf/slice_the_pie.R")
data = function(file){
for (i in file) {
print(i)
}
}
data("hello.txt")
data = function(file){
read = read.table(file ,  header = FALSE)
for (i in read){
print(i)
}
}
data("hello.txt")
library(optparse)
source("mk_vcf/slice_the_pie.R")
source("mk_vcf/slice_the_pie.R")
getwd()
source("slice_the_pie.R")
source("slice_the_pie.R")
source("slice_the_pie.R")
library(ShortRead)
library(seqinr)
library(ShortRead)
seperate_fastq <- function(file, num_files) {
# Read FASTQ paths
paths <- readLines(file)
r1_file <- grep("_R1_", paths, value = TRUE)
r2_file <- grep("_R2_", paths, value = TRUE)
if (length(r1_file) != 1 || length(r2_file) != 1) {
stop("Expected exactly one R1 and one R2 file")
}
# -------- Count reads from R1 only --------
con <- file(r1_file, "r")
lines <- 0
repeat {
z <- readLines(con, n = 100000)
if (length(z) == 0) break
lines <- lines + length(z)
}
close(con)
if (lines %% 4 != 0) stop("Invalid FASTQ format")
reads <- lines / 4
reads_per_sample <- as.integer(reads / num_files)
cat("Total reads:", reads, "\n")
cat("Reads per split file:", reads_per_sample, "\n\n")
# -------- Prepare output --------
output_dir <- "~/chunk"
dir.create(output_dir, showWarnings = FALSE)
suffix <- "AGRF"
sample_ids <- sprintf("XY%03d-%s", seq_len(num_files), suffix)
# -------- Streaming --------
stream1 <- FastqStreamer(r1_file, n = reads_per_sample)
stream2 <- FastqStreamer(r2_file, n = reads_per_sample)
for (sample in sample_ids) {
fq1 <- yield(stream1)
fq2 <- yield(stream2)
if (length(fq1) == 0) break
writeFastq(fq1, file.path(output_dir, paste0(sample, "_R1.fastq")))
writeFastq(fq2, file.path(output_dir, paste0(sample, "_R2.fastq")))
cat("Written", sample, "with", length(fq1), "reads\n")
}
close(stream1)
close(stream2)
}
source("~/mk_vcf/slice_the_pie.R")
source("~/mk_vcf/slice_the_pie.R")
source("slice_the_pie.R")
number = list(make_option(c("-n", "--number"),
type = "integer",
default = 1,
help = "Number of files"),
makeoption(c("-p" , "--path"),
type= "character",
help = "File containing FASTQ paths"))
library(optparse)
number = list(make_option(c("-n", "--number"),
type = "integer",
default = 1,
help = "Number of files"),
makeoption(c("-p" , "--path"),
type= "character",
help = "File containing FASTQ paths"))
number = list(make_option(c("-n", "--number"),
type = "integer",
default = 1,
help = "Number of files"),
make_option(c("-p" , "--path"),
type= "character",
help = "File containing FASTQ paths"))
parser <- OptionParser(option_list = option_list)
opts <- parse_args(parser)
Flags = list(make_option(c("-n", "--number"),
type = "integer",
default = 1,
help = "Number of files"),
make_option(c("-p" , "--path"),
type= "character",
help = "File containing FASTQ paths"))
parser <- OptionParser(option_list = option_list)
parser <- OptionParser(option_list = flags)
parser <- OptionParser(option_list = Flags)
opts <- parse_args(parser)
